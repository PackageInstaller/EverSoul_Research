namespace SkillTarget;

table Deserialization {
    no: int32;
    target_anchor: int32;
    collider_type: int32;
    depth: float;
    width: float;
    offset_depth: float;
    offset_width: float;
    angle: float;
}

table SkillTarget {
    json: [Deserialization];
}

root_type SkillTarget;
