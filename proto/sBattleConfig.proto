syntax = "proto3";

package EsPb;

message sBattleConfig {
  bool autoActive = 1;
  bool autoUltimate = 2;
  repeated sHeroActionPriority heroActionPriority = 3;
}

message sHeroActionPriority {
  int32 heroNo = 1;
  int32 priority = 2;
}

