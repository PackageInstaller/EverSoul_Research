namespace LoginPageSchedule;

table Deserialization {
    no: int32;
    login_pathOffset: string;
    start_dateOffset: string;
    end_dateOffset: string;
}

table LoginPageSchedule {
    json: [Deserialization];
}

root_type LoginPageSchedule;
