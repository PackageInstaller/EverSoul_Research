namespace Thumbnail;

table Deserialization {
    no: int32;
    item_kindOffset: string;
    icon_pathOffset: string;
    order: int32;
}

table Thumbnail {
    json: [Deserialization];
}

root_type Thumbnail;
