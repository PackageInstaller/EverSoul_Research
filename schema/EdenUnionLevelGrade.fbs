namespace Tbl;

table EdenUnionLevelGrade {
  index: int32;
  level: int32;
  value: float;
}

table EdenUnionLevelGradeTable {
  eden_union_level_grades: [EdenUnionLevelGrade];
}

root_type EdenUnionLevelGradeTable;
