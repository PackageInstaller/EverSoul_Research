namespace EdenUnionLevelGrade;

table Deserialization {
    index: int32;
    level: int32;
    value: float;
}

table EdenUnionLevelGrade {
    json: [Deserialization];
}

root_type EdenUnionLevelGrade;
