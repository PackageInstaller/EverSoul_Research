namespace TownWarp;

table Deserialization {
    no: int32;
    group: int32;
    button_sno: int32;
    positionOffset: string;
    section_limit: int32;
    imgOffset: string;
}

table TownWarp {
    json: [Deserialization];
}

root_type TownWarp;
