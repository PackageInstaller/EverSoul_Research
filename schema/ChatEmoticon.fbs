namespace Tbl;

table ChatEmoticon {
  no: int32;
  tab_no: int32;
  sort_no: int32;
  img: string;
  use_anim: int32;
}

table ChatEmoticonTable {
  chat_emoticons: [ChatEmoticon];
}

root_type ChatEmoticonTable;
