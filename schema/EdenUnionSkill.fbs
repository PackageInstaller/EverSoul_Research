namespace EdenUnionSkill;

table Deserialization {
    no: int32;
    hero_no: int32;
    skill_no: int32;
    constraint_level1Offset: string;
    constraint_level2Offset: string;
    constraint_level3Offset: string;
    constraint_level4Offset: string;
    constraint_level5Offset: string;
}

table EdenUnionSkill {
    json: [Deserialization];
}

root_type EdenUnionSkill;
