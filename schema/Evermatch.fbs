namespace Evermatch;

table Deserialization {
    no: int32;
    group_no: int32;
    stage_no: int32;
    actor_no: int32;
    img_path: string;
    start_date: string;
}

table Evermatch {
    json: [Deserialization];
}

root_type Evermatch;
