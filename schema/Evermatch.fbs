namespace Evermatch;

table Deserialization {
    no: int32;
    group_no: int32;
    stage_no: int32;
    actor_no: int32;
    img_pathOffset: string;
    start_dateOffset: string;
}

table Evermatch {
    json: [Deserialization];
}

root_type Evermatch;
