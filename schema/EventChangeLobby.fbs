namespace EventChangeLobby;

table Deserialization {
    no: int32;
    hero_no: int32;
    hero_scale: float;
    background_nameOffset: string;
    bgm_nameOffset: string;
    start_dateOffset: string;
    end_dateOffset: string;
    apply_new_account: int32;
}

table EventChangeLobby {
    json: [Deserialization];
}

root_type EventChangeLobby;
