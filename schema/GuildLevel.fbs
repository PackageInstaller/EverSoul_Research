namespace GuildLevel;

table Deserialization {
    no: int32;
    need_exp: int32;
    sum_exp: int32;
    max_member: int32;
    have_skill_point: int32;
}

table GuildLevel {
    json: [Deserialization];
}

root_type GuildLevel;
